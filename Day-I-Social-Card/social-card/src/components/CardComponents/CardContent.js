import React from 'react';
import './Card.css';

const CardContent = () => {
    return (
        <div className="cardContent">
        <h3>Get started with React</h3>
        <p>React makes it painless to create interactive UI's. Design simplease views for each state in your application</p>
        <a href="https://www.reactjs.org">reactjs.org"</a>
        </div>
    );
};

export default CardContent;