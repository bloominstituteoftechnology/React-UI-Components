import React from 'react';
import './Card.css';

const CardContent = () => {
  return(
    <div className="card-content">
      <h4>Get Started with React</h4>
      <p>React makes it to create interactive UIs. Design Simple views for each state in your application</p>
    </div>
  );
};

export default CardContent;
