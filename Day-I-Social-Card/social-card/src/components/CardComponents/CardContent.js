import React from 'react';
import './Card.css';

const CardContent = ()=> (
    <div class='cardcontent'>
        <h2 class='getstarted'>Get started with React</h2>
        <p class='cardp'>React makes it painless to create interactive UIs. Design simple views for each state in your application.</p>
        <p class='url'>reactjs.org</p>
    </div>
);

export default CardContent;
