import React from 'react';
import './Card.css';

const CardContent = () => {
    return (
    <div>
        <span className="card-title">Get started with React</span>
        <span className="card-paragraph">React makes it painless to create interactive UIs. Design simple views for each state in your application.</span>
        <span className="card-link">reactjs.org</span>
    </div>
    );
};

export default CardContent
