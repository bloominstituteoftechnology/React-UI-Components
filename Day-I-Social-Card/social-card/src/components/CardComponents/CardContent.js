import React from 'react';
import './Card.css';

const CardContent = () => {
    return (
        <div class="Card-Text">
            <h1>Get started with React</h1>
            <p class="text">React makes it painless to create interactive UIs. Design simple views for each state in your application.</p>
            <p class="gray">reactjs.org</p>
        </div>
    )
};

export default CardContent;