import React from 'react';
import './Card.css';

const CardContent = () => {
    return (
        <div class="cardText">
            <p class="tweetLinkHeadline">Get Started with React</p>
            <p class="tweetLinkText">React makes it painless to create interactive UIs. Design simple views for each state in your application.</p>
            <p class="tweetLinkLink grey">reactjs.org</p>
        </div>
    )
}

export default CardContent;