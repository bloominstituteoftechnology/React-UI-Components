import React from 'react';
import './Card.css';

const CardContent = () => {
	return (
		<div className="indent">
			<h2>Get started with React</h2>
      <p className="cardP">React makes it painless to create interactive UIs. Design simple views for each state in your application.</p>
      <a href="http://reactjs.org">reactjs.org</a>
		</div>
	)
};

export default CardContent;
