import React from 'react';
import './Card.css';

const CardContent = () => {
    return (
    <div className="card-text">
        <p className="card-title">Get started with React</p>
        <p className="card-paragraph">React makes it painless to create interactive UIs. Design simple views for each state in your application.</p>
        <p className="card-link">reactjs.org</p>
    </div>
    );
};

export default CardContent
