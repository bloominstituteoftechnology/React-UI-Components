import React from 'react';
import './Card.css';

const CardContent = ()=> {
    return (
        <div class="card-content">
            <h2>Get Started with React</h2>
            <p>React makes it painless to create inteactive UIs. Design simple views for each state in your application.</p>
            <small><p>react.js.org</p></small>
        </div>
    );
};

export default CardContent;