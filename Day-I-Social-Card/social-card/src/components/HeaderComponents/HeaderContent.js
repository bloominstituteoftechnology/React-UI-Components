import React from 'react';
import './Header.css';

const HeaderContent = props => {
  return (
    <div className="header-txt">
      Let's learn React by building simple interfaces with components. Don't try to overthink it, just keep it simple and have fun. Once you feel comfortable using components you are well on the way to mastering React!
    </div>
  );
};

export default HeaderContent;
