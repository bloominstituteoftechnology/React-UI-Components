import React from 'react';
import './Header.css';

const HeaderContent = props => {
    return (
        <div className="header-content">And there you have it. This will set you up for the proper React Syntax that is considered Best Practices today. If you were to encounter a React Application without JSX it would be most likely be very difficult to read and reason about.</div>
    );
}

export default HeaderContent;