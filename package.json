{
  "name": "react1",
  "version": "1.0.0",
  "description": "babel transform and then serve",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx babel src -d lib --presets=env,react",
    "serve": "npx serve .",
    "watch": "npx babel src -d lib --presets=env,react -w",
    "dev": "npm run watch & npm run serve"
  },
  "author": "@axhon",
  "license": "ISC",
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "serve": "^6.5.3"
  }
}
