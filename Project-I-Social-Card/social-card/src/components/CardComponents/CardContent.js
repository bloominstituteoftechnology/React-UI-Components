import React from "react"
import "./Card.css"

const CardContent = () => {
  return (
    <section>
      <h2>Get started with React</h2>
      <p>
        React makes it painless to create interactive UIs. Design simple views
        for each state in your applicaiton.
      </p>
      <p>reactjs.org</p>
    </section>
  )
}

export default CardContent
