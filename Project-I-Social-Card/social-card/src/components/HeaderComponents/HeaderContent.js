import React from "react";
import "./Header.css";

const HeaderContent = () => {
  return (
    <p className = 'content'>
      Let's learn React by building simple interfaces with components. Don't try
      and overthink it, just keep it simple and have fun. Once you feel
      comfortable with using components you are well on your way to mastering
      React!
    </p>
  );
};

export default HeaderContent;
