import React from 'react';
import './Header.css';

const HeaderTitle = () => {
    //let today = Date.now();
    //let monthAndDay = `${today.getDate()} ${today.getMonth() + 1}`;
    return <h1>Lambda School <span>@Lambda School - 6 Jan</span></h1>;
}

export default HeaderTitle;
