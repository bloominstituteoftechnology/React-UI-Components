import React from 'react';
import './Button.css';

const ActionButton = props => {
    return (
        <div>
            <p>we are ACTION buttons</p>
            <p>we are ACTION buttons</p>
            <p>we are ACTION buttons</p>
            <p>we are ACTION buttons</p>
        </div>
    ) 
}

export default ActionButton;
