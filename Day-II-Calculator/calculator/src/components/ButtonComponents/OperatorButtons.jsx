import React from 'react';

const OperatorButtons = props => {
    return (
        <>
            <button className="operator-buttons">{props.text}</button>
        </>
    );
};

export default OperatorButtons;
