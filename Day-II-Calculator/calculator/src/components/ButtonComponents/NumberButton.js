import React from 'react';
import './Button.css';

const FunctionButtons = props => {
    return (
        <div>
            
        </div>
    );
};

const NumButton = props => {
    return (
        <div>
            Number buttons here
        </div>
    );
};

export default NumButton;