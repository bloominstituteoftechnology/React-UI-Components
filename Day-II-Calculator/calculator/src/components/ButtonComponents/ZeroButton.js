import React from 'react';
import './Button.css';

const ZeroButton = (props) => {
    return (
        <div className="bigButton">
            0000
        </div>
    );
};

export default ZeroButton;

