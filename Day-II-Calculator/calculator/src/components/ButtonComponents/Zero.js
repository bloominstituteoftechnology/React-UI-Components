import React from 'react';
import '../../less/Button.less';

function ZeroButton(props) {
    return (
        <React.Fragment>
            <div>{props.zero}</div>
        </React.Fragment>
    )
}

export default ZeroButton;