import React from 'react';
import './Button.css';

const ClearButton = (props) => {
    return (
        <div className="bigButton">
        Clear
        </div>
    );
};

export default ClearButton;